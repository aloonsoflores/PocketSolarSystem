/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['apod.nasa.gov', 'images-assets.nasa.gov', 'www.youtube.com', 'mars.jpl.nasa.gov', 'mars.nasa.gov'],
    }
};

export default nextConfig;
